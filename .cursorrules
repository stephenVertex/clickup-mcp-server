Important:Every time we change the code we need to do the following:

- rebuild with npm run build
- install with npm install -g .
- Kill the current node.js process with pkill -9 -f clickup-mcp-server so I can restart it
- if applicable, update the checklist.md file